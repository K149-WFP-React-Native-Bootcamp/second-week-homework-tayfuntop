import { StyleSheet } from 'react-native';
import {themes} from "../../themes/palettes";

const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        backgroundColor: themes.bisque,
    },
});

export default styles;
